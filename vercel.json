{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "pnpm install",
  "buildCommand": "pnpm db:generate && pnpm db:migrate:deploy && pnpm --filter web build",
  "outputDirectory": "apps/web/.next",
  "framework": "nextjs"
}
